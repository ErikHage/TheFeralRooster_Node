<!DOCTYPE html>
<html>
<head>
    <% include partials/headerCommon %>

    <title><%= title %></title>
</head>
<body>

    <!-- Header + Navbar -->
    <% include partials/pageHeader.ejs %>

    <div class="container-fluid">
        <div class="row">
            <div class="col-sm-2"></div>
            <div class="col-sm-8">

                <!-- Fix styling on panels and links in titles -->

                <div class="panel-group panel-group-skills">
                <% for(var i=0; i<skills.length; i++) { %>
                    <div class="panel panel-default panel-skills">
                        <div class="panel-heading panel-heading-skills">
                            <h3 class="panel-title-skills">
                               <a data-toggle="collapse"
                               href="<%= '#skills' + i %>">
                               <%= skills[i].area %></a></h3>
                        </div>
                        <div id="<%= 'skills' + i %>" class="panel-collapse collapse">
                            <div class="panel-body">
                                <ul>
                                <% for(var j=0; j<skills[i].skill.length; j++) { %>
                                    <li> <%= skills[i].skill[j] %> </li>
                                <% } %>
                                </ul>
                            </div>
                        </div>
                    </div>

                <% } %>
                </div>

            </div>
            <div class="col-sm-2"></div>
        </div>
    </div>

</body>
<footer>
    <% include partials/footer %>
</footer>
</html>
